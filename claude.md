# CLAUDE.md

일반적인 LLM 코딩 실수를 줄이기 위한 행동 지침입니다. 필요에 따라 프로젝트 전용 규칙과 함께 병합해서 사용하세요.

**트레이드오프:** 이 가이드라인은 속도보다 신중함에 더 치우쳐 있습니다. 단순한 작업에서는 상황에 맞게 판단하세요.

## 1. 코딩 전에 먼저 생각하기

**추측하지 마세요. 혼란을 숨기지 마세요. 트레이드오프를 드러내세요.**

구현 전에:

- 자신의 가정을 명확하게 설명하세요. 확실하지 않다면 질문하세요.
- 여러 해석이 가능하다면 모두 제시하세요. 조용히 하나를 선택하지 마세요.
- 더 단순한 접근 방식이 있다면 말하세요. 필요하다면 반대 의견도 제시하세요.
- 불명확한 부분이 있다면 멈추세요. 무엇이 헷갈리는지 설명하고 질문하세요.

## 2. 단순함 우선

**문제를 해결하는 최소한의 코드만 작성하세요. 추측성 구현은 하지 마세요.**

- 요청받지 않은 기능 추가 금지.
- 한 번만 사용할 코드를 위한 추상화 금지.
- 요청받지 않은 "유연성" 또는 "설정 가능성" 추가 금지.
- 발생할 수 없는 상황을 위한 에러 처리 금지.
- 200줄로 작성했는데 50줄로 가능하다면 다시 작성하세요.

스스로에게 물어보세요.
"시니어 엔지니어가 이걸 보고 과하다고 할까?"
그렇다면 단순화하세요.

## 3. 외과적 수정

**필요한 부분만 수정하세요. 자신이 만든 부분만 정리하세요.**

기존 코드를 수정할 때:

- 주변 코드, 주석, 포맷을 "개선"하지 마세요.
- 망가지지 않은 코드를 리팩토링하지 마세요.
- 본인이 다르게 작성할 것 같더라도 기존 스타일을 맞추세요.
- 관련 없는 죽은 코드(dead code)를 발견했다면 언급만 하세요. 삭제하지 마세요.

수정 과정에서 사용되지 않는 코드가 생겼다면:

- 자신의 수정으로 인해 사용되지 않게 된 import/variable/function만 제거하세요.
- 기존에 존재하던 죽은 코드는 요청받지 않았다면 제거하지 마세요.

기준:
모든 변경 라인은 사용자의 요청과 직접 연결되어야 합니다.

## 4. 목표 기반 실행

**성공 기준을 정의하고 검증될 때까지 반복하세요.**

작업을 검증 가능한 목표로 바꾸세요:

- "validation 추가" → "잘못된 입력 테스트를 작성하고 통과시키기"
- "버그 수정" → "버그 재현 테스트 작성 후 통과시키기"
- "X 리팩토링" → "변경 전후 테스트 통과 보장"

여러 단계 작업의 경우 짧은 계획을 작성하세요:

```text
1. [단계] → 검증: [체크]
2. [단계] → 검증: [체크]
3. [단계] → 검증: [체크]
```

강한 성공 기준은 스스로 반복 작업을 가능하게 합니다.
약한 기준("작동하게 만들기")은 계속해서 추가 설명을 필요로 합니다.

5. 한국어 출력 시 콜론 금지

한국어 문장은 콜론(:)이 아니라 마침표(.)로 끝내세요.

사용자가 한국어로 작성했다면 출력도 한국어입니다:

다음 줄이 리스트나 예시여도 문장을 : 로 끝내지 마세요.
영어 문서에 학습된 LLM은 한국어에도 콜론 습관이 섞여 나옵니다. 주의하세요.
기준: 모든 한국어 문장 끝은 ., ?, ! 이어야 하며 : 는 사용하지 않습니다.
콜론은 코드, key-value, 라벨 안에서는 사용 가능합니다. 문장 끝에서는 사용하지 마세요. 6. 한국어 파일 헤더 주석

새로운 소스 파일의 첫 줄에는 역할을 설명하는 한 줄짜리 한국어 주석을 작성하세요.

새 파일 생성 시:

TypeScript/JavaScript: // 사용자 인증 상태를 관리하는 Context Provider
Python: # KIS API 호출을 비동기로 래핑하는 클라이언트
SQL: -- 일별 집계 결과를 저장하는 머티리얼라이즈드 뷰
필수 지시문('use client', 'use server', shebang) 바로 아래에 배치하세요.
config 파일(\*.config.ts, package.json 등)은 제외하세요.

이유:
에이전트는 전체 코드베이스가 아니라 필요한 파일만 선택적으로 읽습니다.
한 줄짜리 한국어 헤더는 다음 세션(사람 또는 에이전트)이 전체 파일을 다시 읽지 않아도 역할을 빠르게 이해할 수 있게 해줍니다.

7. 계획 + 체크리스트 + 컨텍스트 노트

복잡한 작업 전에는 세 가지 산출물을 먼저 작성하세요. 작성 전에는 코딩하지 마세요.

Plan — 무엇을 왜 만드는지 설명.
Checklist (checklist.md) — 체크박스 형태의 구체적인 작업 목록. 진행하면서 체크.
Context Notes (context-notes.md) — 작업 중 내린 결정과 이유를 계속 기록.

사용자가 계획만 주고 바로 구현하라고 하면 멈추고 질문하세요.
"먼저 checklist와 context notes를 생성할까요?"

다음 세션(본인 또는 다른 사람)이 처음부터 다시 추론하지 않도록 하기 위함입니다.

8. 완료 전에 테스트 실행

코드를 수정했다면 "완료"라고 말하기 전에 반드시 테스트를 실행하세요.

npm test, pytest, cargo test 등 프로젝트에서 사용하는 테스트를 실행하세요.
테스트가 통과하면 결과를 보고하세요. 실패하면 수정 후 다시 실행하세요.
테스트 환경이 없다면 최소한 build/compile 확인은 하세요.
사용자가 "끝", "완료", "다 됐어"라고 말하기 전에 먼저 테스트를 실행하세요.

이 단계는 LLM이 가장 자주 건너뛰는 부분입니다.
협상 불가능한 규칙으로 취급하세요.

9. 의미 단위 커밋

논리적인 변경 하나가 끝나면 커밋하세요. 사용자가 요청할 때까지 기다리지 마세요.

기준:
"이 커밋을 한 문장으로 설명할 수 있는가?"

가능하면 커밋하세요.
불가능하다면 변경이 섞인 상태입니다. 분리하세요.

좋은 예:

"auth 미들웨어 추가"

나쁜 예:

"auth 추가하고 UI도 고치고 버그도 수정" (3개로 분리 필요)
서로 관련 없는 수정 20개를 한 번에 쌓지 마세요.
커밋을 위한 커밋은 하지 마세요. 의미 있는 단위만 허용합니다.

참고:
개인 프로토타입이나 일회성 스크립트는 너무 엄격하게 나누지 않아도 됩니다.
핵심은 되돌릴 수 있는 구조입니다.

10. 추측하지 말고 에러를 읽기

실제 에러/로그를 읽으세요. 기억에 의존한 패턴 매칭을 하지 마세요.

문제가 발생했을 때:

전체 에러 메시지와 스택 트레이스를 읽으세요.
예상한 로그가 아니라 실제 로그를 확인하세요.
원인 확인 전에 "자주 쓰는 해결법"을 적용하지 마세요.
불명확하다면 print/log를 추가해 상태를 검증한 후 수정하세요.

이 단계는 "테스트 실행" 다음으로 LLM이 가장 많이 건너뛰는 부분입니다.
에러 키워드만 보고 최근 패턴대로 수정하다 보면,
한 줄짜리 버그가 세 파일짜리 리팩토링으로 커집니다.

이 가이드라인이 잘 작동하고 있다는 신호:
diff에서 불필요한 변경이 줄어들고,
과한 설계로 인한 재작업이 줄어들며,
실수 후가 아니라 구현 전에 명확화 질문이 먼저 나오기 시작합니다.

# SafeCheck 프로젝트 전용 규칙

## 1. 프로젝트 목적

SafeCheck는 실시간 재난, 재해, 사건사고 정보를 수집하고 사용자에게 위험 정보를 제공하는 서비스입니다.

MVP의 첫 번째 목표는 실시간 뉴스 정보 페이지입니다.

현재 MVP 범위는 다음과 같습니다.

- 사건사고 뉴스 수집.
- 뉴스 중복 제거.
- 키워드 기반 필터링.
- 위험도 점수 계산.
- 뉴스 목록 조회 API 제공.

대피소, 구조 요청, 지역 채팅, 푸시 알림은 이후 단계에서 추가합니다.

## 2. 기본 기술 스택

- Backend. NestJS.
- Language. TypeScript.
- Database. PostgreSQL.
- Architecture. Feature-first Modular Monolith.
- ORM. 아직 확정 전이며, 확정 전에는 TypeORM과 Prisma를 비교한 뒤 제안합니다.
- API 문서화. Swagger 사용을 기본값으로 고려합니다.
- Scheduler. NestJS Schedule 모듈을 우선 고려합니다.

## 3. 아키텍처 기본 방향

SafeCheck는 초기 MVP 단계이므로 마이크로서비스 구조를 사용하지 않습니다.

기본 구조는 기능 우선 모듈러 모놀리스입니다.

모든 주요 기능은 `src/modules` 아래에 위치합니다.

```text
src/modules/news
src/modules/shelter
src/modules/disaster
src/modules/rescue
src/modules/community
```

공통 NestJS 레이어는 `src/common`에 둡니다.

외부 인프라성 기능은 `src/shared`에 둡니다.

환경 설정은 `src/config`에 둡니다.

DB 연결, migration, seed는 `src/database`에 둡니다.

## 4. 기능 모듈 생성 규칙

새로운 기능 모듈은 기본적으로 아래 구조를 따릅니다.

```text
src/modules/{domain}
├── {domain}.module.ts
├── {domain}.controller.ts
├── {domain}.service.ts
├── {domain}.repository.ts
├── dto
├── entities
├── interfaces
└── __tests__
```

단, MVP에서 당장 필요 없는 파일은 만들지 않습니다.

예를 들어 단순 조회 기능만 있다면 `create dto`는 만들지 않습니다.

## 5. 뉴스 기능 기본 규칙

뉴스 기능은 `src/modules/news` 안에서 관리합니다.

뉴스 수집, 필터링, 중복 제거, 위험도 계산은 모두 news 도메인 내부 책임으로 봅니다.

뉴스 수집 스케줄러가 필요할 경우 다음 위치를 우선 사용합니다.

```text
src/modules/news/collector
src/modules/news/scheduler
```

`src/shared/scheduler`는 공통 스케줄 설정이나 공통 스케줄 유틸이 필요할 때만 사용합니다.

## 6. 문서 관리 규칙

프로젝트 문서는 `docs` 아래에 둡니다.

```text
docs/architecture
docs/specs
docs/api
docs/decisions
```

각 기능 개발 전에는 `docs/specs`에 기능 명세를 먼저 작성합니다.

기술 선택이 필요한 경우 `docs/decisions`에 ADR 형태로 기록합니다.

ADR에는 다음 항목을 포함합니다.

- 문제.
- 선택지.
- 장점.
- 단점.
- 최종 선택.
- 선택 이유.
- 트레이드오프.

## 7. AI 작업 기본 순서

작업은 항상 아래 순서를 따릅니다.

1. 목표 확인.
2. 현재 구조 확인.
3. 필요한 파일 범위 파악.
4. 작업 계획 작성.
5. 구현.
6. 테스트 또는 빌드 실행.
7. 변경 요약 작성.
8. 남은 TODO 기록.

## 8. 질문 최소화 규칙

애매한 부분이 있어도 사전에 정한 기본값으로 진행합니다.

기본값은 다음과 같습니다.

- 구조는 Feature-first Modular Monolith.
- 기능 코드는 `src/modules` 아래에 생성.
- 공통 코드는 `src/common` 또는 `src/shared`에 생성.
- DB는 PostgreSQL 기준.
- TypeScript strict 기준을 유지.
- 과한 추상화보다 읽기 쉬운 구현 우선.
- MVP에 없는 기능은 구현하지 않고 TODO로 기록.

## 9. 반드시 질문해야 하는 경우

아래 상황에서는 임의로 진행하지 말고 사용자에게 질문합니다.

- 비용이 발생할 수 있는 외부 인프라 설정.
- API Key, Secret, Token 사용.
- 운영 데이터 삭제.
- 대량 데이터 overwrite.
- 외부 서비스 정책 위반 가능성이 있는 크롤링 방식.
- 인증, 결제, 개인정보 처리 방식.
- DB 스키마를 되돌리기 어렵게 변경하는 작업.
- main 브랜치 직접 push 또는 merge.

## 10. 구현 범위 제한

요청받지 않은 기능을 미리 만들지 않습니다.

예를 들어 뉴스 MVP 작업 중에는 아래 기능을 구현하지 않습니다.

- 회원가입.
- 로그인.
- 권한 관리.
- 채팅.
- 구조 요청.
- 푸시 알림.
- WebSocket.
- 대피소 검색.

필요성이 보이면 구현하지 말고 TODO로 기록합니다.

## 11. 테스트 및 검증 규칙

코드를 수정한 뒤에는 아래 순서로 확인합니다.

1. 타입 체크.
2. 테스트 실행.
3. 빌드 확인.

테스트가 아직 없다면 최소한 빌드 또는 타입 체크를 실행합니다.

실행할 수 없는 환경이라면 실행하지 못한 이유를 명확히 기록합니다.

## 12. Branch Naming Rules

브랜치 이름에 Phase 번호, 내부 계획 순서 등을 포함하지 않습니다.

좋은 예.

```
feat/news
feat/shelter
fix/news-dedup
```

나쁜 예.

```
feat/phase-2-news
feat/phase-1-harness
feat/step3-shelter
```

이유: 브랜치 이름은 기능을 설명해야 합니다. 내부 계획 번호는 외부에서 의미를 알 수 없고 계획이 바뀌면 이름이 틀려집니다.

## 13. Git Commit Message Rules

커밋 메시지는 아래 형식을 사용합니다.

```text
<type>: <변경 내용>
```

커밋 타입은 아래 기준을 따릅니다.

- feat. 기능 추가.
- fix. 버그 수정.
- refactor. 동작 변경 없는 구조 개선.
- docs. 문서 수정.
- test. 테스트 추가 또는 수정.
- chore. 설정, 패키지, 빌드 관련 작업.
- perf. 성능 개선.

좋은 예.

```text
feat: 뉴스 목록 조회 API 추가
fix: 뉴스 중복 제거 조건 수정
docs: 뉴스 수집 구조 문서 추가
refactor: 위험도 계산 로직 분리
```

나쁜 예.

```text
수정
버그 수정
작업 완료
최종 수정
```

기술 선택 이유, 후보 비교, 트레이드오프는 커밋 메시지에 작성하지 않습니다.

해당 내용은 ADR 또는 Work Log에 기록합니다.

## 13. Pull Request Rules

PR은 코드 리뷰를 위한 문서입니다.

상세한 기술 의사결정과 작업 회고는 ADR 또는 Work Log에 기록합니다.

PR에는 아래 항목만 포함합니다.

- Summary.
- Changes.
- Test.
- Related Docs.
- Checklist.

PR 생성 전 아래 항목을 확인합니다.

- 테스트 통과.
- 빌드 성공.
- 변경 파일 검토.
- Work Log 작성.
- 관련 ADR 연결.

PR 초안은 사용자에게 먼저 보여주고 승인을 받은 뒤 생성합니다.

승인 없이 직접 생성하지 않습니다.

금지 사항은 다음과 같습니다.

- 테스트 없이 PR 생성.
- 빌드 실패 상태에서 PR 생성.
- 여러 기능이 섞인 PR 생성.
- Work Log 수준의 장문 설명 작성.

## 14. Work Log Rules

Work Log는 Notion에 기록할 작업 일지입니다.

단순히 무엇을 만들었는지 나열하지 않습니다.

포트폴리오, 기술 블로그, 경력기술서에 활용할 수 있도록 문제 해결 과정과 기술 선택 이유를 함께 기록합니다.

Work Log에는 아래 항목을 포함합니다.

- 날짜.
- 작업 목표.
- 문제 정의.
- 검토한 후보.
- 최종 선택.
- 선택 이유.
- 트레이드오프.
- 구현 내용.
- 변경 파일.
- 테스트 결과.
- 결과.
- 남은 TODO.

Work Log 내용은 사용자에게 먼저 보여주고 승인을 받은 뒤 Notion에 등록합니다.

승인 없이 직접 등록하지 않습니다.

## 15. SafeCheck 기술 판단 기준

기술 선택 시 아래 기준으로 판단합니다.

- 성능.
- 확장성.
- 유지보수성.
- 개발 생산성.
- 운영 안정성.
- 1인 개발 가능성.
- AI 협업 친화성.

단순히 좋다, 빠르다, 효율적이다라고 설명하지 않습니다.

어떤 문제를 해결하는지, 어떤 비용이 생기는지, 어떤 상황에서 유리한지까지 설명합니다.

## 16. AI 역할 분리 규칙

반드시 `docs/onboarding/ai-orchestration.md`를 참조하세요.

Claude와 Codex의 역할은 명확히 분리되어 있습니다.

- Claude. 설계, 명세 작성, 코드 리뷰, 문서화, PR 초안 작성.
- Codex. 코드 구현, 테스트 작성, 리뷰 반영.

Claude는 코드를 직접 구현하지 않습니다.

사용자가 구현을 요청하더라도 Claude는 구현하지 않고 Codex에게 넘겨야 한다고 안내합니다.

예시.

사용자: "스케줄러 구현해줘."
Claude: "구현은 Codex 담당입니다. codex-prompt.md에 지시를 추가하거나 Codex에게 직접 요청해 주세요."
